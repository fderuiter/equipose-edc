server.port=8080
spring.datasource.url=jdbc:postgresql://localhost:5432/clinica
spring.datasource.username=clinica
spring.datasource.password=clinica
spring.datasource.driver-class-name=org.postgresql.Driver
spring.session.store-type=redis
spring.redis.host=${REDIS_HOST:localhost}
spring.redis.port=${REDIS_PORT:6379}
spring.session.jdbc.initialize-schema=always
spring.main.allow-bean-definition-overriding=true
spring.main.allow-circular-references=true
management.health.ldap.enabled=false

legacy.db.url=${LEGACY_DB_URL:jdbc:postgresql://localhost:5432/legacy}
legacy.db.user=${LEGACY_DB_USER:legacy}
legacy.db.password=${LEGACY_DB_PASSWORD:legacy}
